# Next.js + npm workspaces: peer deps (e.g. react/react-dom versions) resolve
# more reliably with the legacy peer-deps algorithm. This only affects root
# `npm install`; the per-app Docker builds use their own isolated installs.
legacy-peer-deps=true
