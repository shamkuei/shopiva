/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Produce a self-contained `.next/standalone` server for the production image.
  output: 'standalone',
};

export default nextConfig;
